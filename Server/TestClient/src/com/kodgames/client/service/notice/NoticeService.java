package com.kodgames.client.service.notice;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import com.kodgames.corgi.core.service.PublicService;

public class NoticeService extends PublicService
{
	private static final long serialVersionUId = -325304891050756055L;
	Logger logger = LoggerFactory.getLogger(NoticeService.class);

//	public void noticeResult(GCNoticeRES res)
//	{
//		logger.error("content={}", res.getContent());
//		logger.error("sender={}", res.getSender());
//		long effectiveTime = Long.parseLong(res.getEffectiveTime());
//		logger.error("effective_time={}", new Date(effectiveTime));
//	}
}
